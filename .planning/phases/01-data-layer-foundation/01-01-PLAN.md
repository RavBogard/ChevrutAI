---
phase: 01-data-layer-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/services/sefaria.js
  - src/test/sefaria.test.js
autonomous: true
requirements:
  - DATA-03

must_haves:
  truths:
    - "flattenSefariaText is exported from src/services/sefaria.js"
    - "All 6 Sefaria JaggedArray depths (string, string[], string[][], string[][][], null/empty, sparse arrays with null gaps) are tested and passing"
    - "No runtime error occurs when normalization receives any of the 6 fixture inputs"
  artifacts:
    - path: "src/services/sefaria.js"
      provides: "Exported flattenSefariaText function (renamed/promoted from private normalizeText)"
      exports: ["flattenSefariaText"]
    - path: "src/test/sefaria.test.js"
      provides: "Unit test suite with 6 fixture-driven tests for flattenSefariaText"
      contains: "describe('flattenSefariaText'"
  key_links:
    - from: "src/test/sefaria.test.js"
      to: "src/services/sefaria.js"
      via: "named import"
      pattern: "import.*flattenSefariaText.*from.*services/sefaria"
---

<objective>
Export and test the existing Sefaria text normalization function against all JaggedArray depths used in production.

Purpose: DATA-03 requires that text normalization handles Talmud (string[][]), Zohar (string[][][]), and Mishnah (string[]) without runtime errors. The recursive algorithm already exists as the private `normalizeText()` function in sefaria.js — it is algorithmically correct but completely untested. This plan extracts it as a public `flattenSefariaText` export and validates it with fixture tests before any store or consumer code depends on it.

Output: Exported `flattenSefariaText` in sefaria.js + passing test suite in src/test/sefaria.test.js.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/dsbog/ChevrutAI/.planning/PROJECT.md
@C:/Users/dsbog/ChevrutAI/.planning/ROADMAP.md
@C:/Users/dsbog/ChevrutAI/.planning/phases/01-data-layer-foundation/01-RESEARCH.md
@C:/Users/dsbog/ChevrutAI/src/services/sefaria.js
@C:/Users/dsbog/ChevrutAI/src/test/setup.js
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing tests for flattenSefariaText (RED)</name>
  <files>src/test/sefaria.test.js</files>
  <behavior>
    - Test 1: flat string input → returns the string unchanged (Tanakh single verse)
    - Test 2: string[] input → joins with single space (Tanakh verse range, Mishnah)
    - Test 3: string[][] input → flattens two levels and joins (Talmud amud)
    - Test 4: string[][][] input → flattens three levels and joins (Zohar section)
    - Test 5: null, undefined, and empty string → return '' without throwing (never crash)
    - Test 6: array with null/undefined gap nodes → filters them out, returns non-null text only (Sefaria gap nodes)
  </behavior>
  <action>
    Create src/test/sefaria.test.js. Import flattenSefariaText from '../services/sefaria'. Write 6 describe/it tests using fixtures that match actual Sefaria API response shapes per the research doc.

    Use exactly these fixture shapes (from 01-RESEARCH.md Pattern 5):
    - Test 1: flattenSefariaText('בְּרֵאשִׁית') → 'בְּרֵאשִׁית'
    - Test 2: flattenSefariaText(['verse 1', 'verse 2']) → 'verse 1 verse 2'
    - Test 3: flattenSefariaText([['line 1a', 'line 1b'], ['line 2a']]) → 'line 1a line 1b line 2a'
    - Test 4: flattenSefariaText([[['para 1a', 'para 1b'], ['para 2a']]]) → 'para 1a para 1b para 2a'
    - Test 5: null→'', undefined→'', ''→''  (three separate expect calls in one it block)
    - Test 6: flattenSefariaText([null, 'text', undefined]) → 'text'

    Run the tests. They MUST fail with "flattenSefariaText is not a function" or similar import error — this confirms RED state.

    Commit: test(01-01): add failing flattenSefariaText tests
  </action>
  <verify>
    <automated>cd C:/Users/dsbog/ChevrutAI && npx vitest run src/test/sefaria.test.js 2>&1 | grep -E "FAIL|Cannot find|not a function|import"</automated>
  </verify>
  <done>Tests exist and fail with import/not-a-function error (not assertion errors — the function doesn't exist yet)</done>
</task>

<task type="tdd">
  <name>Task 2: Export flattenSefariaText from sefaria.js (GREEN)</name>
  <files>src/services/sefaria.js</files>
  <behavior>
    - The existing private `normalizeText` function at line 10 is promoted to an exported `flattenSefariaText`
    - The implementation handles: string passthrough, Array.isArray recursive map+filter+join, null/undefined early return of '', unexpected types via String() fallback
    - No change to any other function in sefaria.js — only add the export
  </behavior>
  <action>
    Open src/services/sefaria.js. Find the existing private `normalizeText` function (line ~10). Add a new exported function directly below it:

    ```javascript
    export const flattenSefariaText = (text) => {
      if (!text) return '';
      if (typeof text === 'string') return text;
      if (Array.isArray(text)) {
        return text
          .map(flattenSefariaText)
          .filter(Boolean)
          .join(' ')
          .trim();
      }
      return String(text);
    };
    ```

    DO NOT delete or rename the existing private `normalizeText` function — it is still used internally by getSefariaText and searchSefariaText. The exported version is a parallel addition.

    Run the tests. All 6 MUST pass.

    Commit: feat(01-01): export flattenSefariaText from sefaria.js
  </action>
  <verify>
    <automated>cd C:/Users/dsbog/ChevrutAI && npx vitest run src/test/sefaria.test.js</automated>
  </verify>
  <done>All 6 flattenSefariaText tests pass with 0 failures. npx vitest run exits with code 0.</done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
```
cd C:/Users/dsbog/ChevrutAI && npm run test:run
```
All previously passing tests continue to pass. The 6 new sefaria tests pass.
</verification>

<success_criteria>
- src/test/sefaria.test.js exists with 6 tests
- All 6 tests pass: string, string[], string[][], string[][][], null/empty, sparse array
- flattenSefariaText is exported from src/services/sefaria.js
- The private normalizeText function is unchanged and still used internally
- npm run test:run exits 0 with no failures
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-layer-foundation/01-01-SUMMARY.md`
</output>

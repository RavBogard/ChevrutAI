---
phase: 01-data-layer-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/stores/useSheetStore.js
  - src/test/sheetStore.test.js
autonomous: true
requirements:
  - DATA-01
  - DATA-04

must_haves:
  truths:
    - "A single useSheetStore module exists as the authoritative sheet state container"
    - "addSource, removeSource, reorderSources, setTitle, loadSheet, resetSheet all work correctly"
    - "Undo/redo correctly tracks add/remove/reorder/setTitle mutations and does NOT track isSaving, isLoading, or isDirty changes"
    - "canUndo and canRedo reflect correct state after mutations"
    - "zustand@5.0.11 and zundo@2.3.0 are installed"
  artifacts:
    - path: "src/stores/useSheetStore.js"
      provides: "Zustand store with temporal middleware, all sheet actions, subscribeWithSelector for autosave"
      exports: ["default (useSheetStore)"]
    - path: "src/test/sheetStore.test.js"
      provides: "Unit tests for store actions and undo/redo behavior"
      contains: "describe('useSheetStore'"
  key_links:
    - from: "src/test/sheetStore.test.js"
      to: "src/stores/useSheetStore.js"
      via: "default import"
      pattern: "import useSheetStore from.*stores/useSheetStore"
    - from: "src/stores/useSheetStore.js"
      to: "zustand temporal middleware"
      via: "temporal() wrapper"
      pattern: "temporal\\("
---

<objective>
Install Zustand and zundo, create the useSheetStore with temporal undo/redo middleware, and write unit tests covering all store actions and undo/redo behavior.

Purpose: This is the structural core of Phase 1. All subsequent plans depend on this store existing. The store replaces the 744-line useSheetPersistence monolith as the single source of truth for sheet state. EditorContainer migration (Plan 04) cannot start until this plan's artifacts exist.

Output: src/stores/useSheetStore.js with Zustand 5 + zundo 2.3.0, full test coverage in src/test/sheetStore.test.js.
</objective>

<execution_context>
@C:/Users/dsbog/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dsbog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/dsbog/ChevrutAI/.planning/PROJECT.md
@C:/Users/dsbog/ChevrutAI/.planning/ROADMAP.md
@C:/Users/dsbog/ChevrutAI/.planning/phases/01-data-layer-foundation/01-RESEARCH.md
@C:/Users/dsbog/ChevrutAI/src/test/setup.js
@C:/Users/dsbog/ChevrutAI/src/test/useUndoRedo.test.js
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies and create useSheetStore</name>
  <files>package.json, package-lock.json, src/stores/useSheetStore.js</files>
  <behavior>
    - zustand@5.0.11 and zundo@2.3.0 appear in package.json dependencies
    - useSheetStore exports a default Zustand store
    - Store state shape: title (string), sources (array), currentSheetId (string|null), isSaving, isLoading, isDirty, isPersisted (all boolean)
    - Actions: addSource(source), removeSource(index), updateSource(index, updates), reorderSources(newArray), setTitle(title), setCurrentSheetId(id), setIsSaving(v), setIsLoading(v), setIsDirty(v), setIsPersisted(v), loadSheet(sheetData), resetSheet()
    - zundo partialize limits undo history to {sources, title} only — isSaving/isLoading/isDirty/isPersisted are NOT in undo history
    - zundo limit: 50 entries
    - Middleware order: subscribeWithSelector(devtools(temporal(...)))
  </behavior>
  <action>
    1. Install packages:
       ```
       cd C:/Users/dsbog/ChevrutAI && npm install zustand@5.0.11 zundo@2.3.0
       ```

    2. Create src/stores/ directory, then create src/stores/useSheetStore.js using the exact pattern from 01-RESEARCH.md (Pattern 1). Key implementation rules:

       - Import: create from 'zustand', temporal from 'zundo', subscribeWithSelector and devtools from 'zustand/middleware'
       - Middleware nesting order: subscribeWithSelector(devtools(temporal(fn, zundiOpts)))
       - loadSheet applies defensive defaults: title ?? 'New Source Sheet', sources ?? [], currentSheetId: sheetData.id ?? null
       - resetSheet sets title back to 'New Source Sheet', sources to [], currentSheetId/isPersisted/isDirty to falsy
       - devtools only in development — wrap: `process.env.NODE_ENV === 'production' ? temporal(...) : subscribeWithSelector(devtools(temporal(...)))`
         Actually: always use devtools wrapper — it is a no-op in production builds when Redux DevTools extension is absent
       - zundo options: `{ partialize: (state) => ({ sources: state.sources, title: state.title }), limit: 50 }`

    3. Export as default: `export default useSheetStore;`

    DO NOT put Firestore logic inside any store action. Store actions are pure state transitions only (per research anti-patterns).
    DO NOT use lodash — the autosave debounce (in Plan 04) uses setTimeout/clearTimeout hand-rolled pattern.
  </action>
  <verify>
    <automated>cd C:/Users/dsbog/ChevrutAI && node -e "import('./src/stores/useSheetStore.js').then(m => console.log('store exports:', Object.keys(m))).catch(e => console.error(e))" 2>&1 || echo "ESM check — run vitest instead"</automated>
  </verify>
  <done>src/stores/useSheetStore.js exists; zustand and zundo appear in package.json dependencies section</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write and pass store unit tests</name>
  <files>src/test/sheetStore.test.js</files>
  <behavior>
    - addSource appends a source object to the sources array
    - removeSource(0) removes the first source; removeSource(1) removes the second
    - reorderSources replaces the sources array entirely with the new array
    - setTitle updates the title string
    - loadSheet populates title, sources, currentSheetId, isPersisted=true, isLoading=false; applies ?? defaults for missing fields
    - resetSheet resets title to 'New Source Sheet', sources to [], currentSheetId to null, isPersisted to false
    - After addSource: temporal pastStates.length > 0 (undo history recorded)
    - After undo: sources array reverts to pre-add state
    - After redo: sources array returns to post-add state
    - setIsSaving(true) does NOT increment temporal pastStates.length (status flags excluded from history)
    - After removeSource then undo: removed source is back in sources array
    - After reorderSources then undo: sources array reverts to pre-reorder order
    - After updateSource(0, { commentary: 'new text' }) then undo: source[0].commentary reverts to its pre-update value (DATA-04 edit commentary coverage)
  </behavior>
  <action>
    Create src/test/sheetStore.test.js. Use Vitest `describe`, `it`, `expect`, `beforeEach`.

    Pattern: Each test calls `useSheetStore.getState().{action}(...)` then asserts `useSheetStore.getState().{field}`.
    For undo/redo: call `useSheetStore.temporal.getState().undo()` then assert state.

    Reset store between tests using `beforeEach`:
    ```javascript
    beforeEach(() => {
      useSheetStore.getState().resetSheet();
      useSheetStore.temporal.getState().clear();
    });
    ```

    For the "status flags excluded from history" test:
    ```javascript
    const { pastStates } = useSheetStore.temporal.getState();
    const before = pastStates.length;
    useSheetStore.getState().setIsSaving(true);
    expect(useSheetStore.temporal.getState().pastStates.length).toBe(before);
    ```

    Run tests after writing. All tests MUST pass before committing.
    ```
    cd C:/Users/dsbog/ChevrutAI && npx vitest run src/test/sheetStore.test.js
    ```
  </action>
  <verify>
    <automated>cd C:/Users/dsbog/ChevrutAI && npx vitest run src/test/sheetStore.test.js</automated>
  </verify>
  <done>All store tests pass. Tests cover: addSource, removeSource, reorderSources, setTitle, updateSource, loadSheet defaults, resetSheet, undo after add, redo after undo, undo after remove, undo after reorder, undo after updateSource (commentary edit), status flag excluded from undo history.</done>
</task>

</tasks>

<verification>
Full test suite passes with no regressions:
```
cd C:/Users/dsbog/ChevrutAI && npm run test:run
```
</verification>

<success_criteria>
- zustand@5.0.11 and zundo@2.3.0 in package.json dependencies
- src/stores/useSheetStore.js exists with all actions and zundo temporal middleware
- zundo partialize excludes isSaving, isLoading, isDirty, isPersisted from undo history
- src/test/sheetStore.test.js exists and all tests pass
- Tests include updateSource undo test (DATA-04 edit commentary coverage)
- npm run test:run exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-layer-foundation/01-02-SUMMARY.md`
</output>
